# OceanSafe shop co-brand pages — clean URLs (rewrite, address bar stays /p/<shop>)
/p/demo         /p/demo.html           200

# Retired "$990/yr Gear partner" demo removed 2026-07-20 — legacy printed URL lands on the current demo
# demo-dashboard.html retired 2026-07-24 — repointed at the current seller-portal demo
/demo/dashboard    /portal-demo/dashboard.html   200
/demo/dashboard/*  /portal-demo/dashboard.html   301

# Conditions proxy + cache (collapses per-visitor open-meteo/NOAA calls into one cached server fetch)
/api/wx         /.netlify/functions/wx  200
/api/creator-provision   /.netlify/functions/creator-provision   200

# Store checkout return (create-square-checkout appends ?order=&pickup= to this URL)
/store/success  /store/success.html  200

# DB-driven partner pages + no-login dashboards (runtime from Supabase)
/p/*  /p.html  200
/d/*  /d.html  200

# Clean island launch URLs for QR codes, cards, email — rewrite (not redirect) so the
# path survives and index.html's resolveIsland() can read the /go/<slug>
/go/*  /index.html  200

# Top-level island URLs (2026-07-18) — oceansafety.app/kauai|oahu|maui. Same rewrite
# mechanism as /go/*: exact paths only, so nothing else at root is shadowed.
/kauai  /index.html  200
/oahu   /index.html  200
/maui   /index.html  200

# Creator ambassador program (2026-07-18)
/creators   /creators.html      200
/@*         /at.html            200
/creator-terms   /creator-terms.html   200
/creator    /creator-dash.html  200
/plan       /plan.html          200
/api/creator-provision   /.netlify/functions/creator-provision   200
/api/creator-config      /.netlify/functions/creator-config      200

# Stay-close (timeshare) CRM/PMS integration (2026-07-19)
/api/ts-lead     /.netlify/functions/ts-lead     200
/api/ts-outcome  /.netlify/functions/ts-outcome  200
/api/ts-cfg      /.netlify/functions/ts-cfg      200
/api/ts-leads/*  /.netlify/functions/ts-leads/:splat  200
/api/ts-promos/* /.netlify/functions/ts-promos/:splat 200
/api/ts-signup   /.netlify/functions/ts-signup   200
/api/ts-upgrade  /.netlify/functions/ts-upgrade  200
/onboard         /stay-close-signup.html         200

# Overview deck — sendable branded link (2026-07-18)
/overview   /overview.pdf   200
/stay-close-brochure   /stay-close-brochure.pdf   200
/timeshare-brochure    /stay-close-brochure.pdf              200
/timeshare             /stay-close-operator-dashboard.html   200

# Wyndham demo link points here — fit it with the new operator console (2026-07-21)
/analytics-console.html   /stay-close-operator-dashboard.html   200

# ── Clean per-segment dashboard URLs (rewrite; address bar stays clean) 2026-07-22 ──
/hotel      /operator-console.html               200
/rental     /rentals-operator-dashboard.html     200
/townad     /townad-demo.html                    200

# ── Shop/seller portal demo — the short link to send a prospect (2026-07-24) ──
# 301, NOT a 200 rewrite like the lines above: those consoles are single files at
# the root, so a rewrite keeps their relative paths working. The portal lives in a
# folder and links to portal.css / app.js / sales.html relatively — served under
# /shopdemo those would resolve to the ROOT and 404. The redirect moves the browser
# into /portal-demo/ so every relative path stays correct. Query strings carry
# through, so /shopdemo?plan=paid still lands on the Paid view.
/shopdemo    /portal-demo/dashboard.html   301
/shop-demo   /portal-demo/dashboard.html   301

# ── Turo hosts / car fleets (2026-07-24) — $5 per vehicle per month ──
/api/car-upgrade   /.netlify/functions/car-upgrade   200
/onboard-cars      /car-signup.html                  200
/cars              /car-providers.html               200
/fleet             /cars-operator-dashboard.html      200
/cars-pdf          /OceanSafe-Cars-OnePager.pdf      200
